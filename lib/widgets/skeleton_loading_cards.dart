
import 'package:flutter/cupertino.dart';

class SkeletonLoadingCards extends StatelessWidget {
  const SkeletonLoadingCards({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Column(
      children: [

      ],
    );
  }
}
